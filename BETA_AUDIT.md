# 中国象棋 AI 教学软件 Beta 审计

审计日期：2026-09-14  
审计原则：只有实际存在并成功执行的能力才标为 PASS；没有实现、没有外部依赖或无法形成端到端证据的一律不通过。

## 结论

**总体结论：NOT BETA / RELEASE BLOCKED。**

当前项目是可启动的 Phase 1 技术预览骨架，不是功能 Beta。它具备模块目录、配置、日志、SQLite 初始化、少量数据模型、UCI 单行解析、导师提示词构建和静态主窗口，但没有可交互棋盘、规则引擎、Pikafish 会话、记谱转换、棋谱播放、沙盘、LLM Provider 实现或训练系统。因此无法完成一盘对局，也不能对 20 个 FEN、20 个导师局面、10 个次优走法、10 盘棋谱做真实验收。

## 已经完成并实际通过的功能

| 功能 | 状态 | 证据 |
|---|---|---|
| Python 包与模块边界 | PASS | 项目可编辑安装，包可导入 |
| SQLite 首次初始化 | PASS | 临时数据库建表测试通过，初始化约 0.018 秒 |
| 基础坐标走法往返 | PASS-LIMITED | 仅 `h2e2` 等引擎坐标结构，不含规则与中文记谱 |
| UCI `info` 单行解析 | PASS-LIMITED | 单条 MultiPV 示例测试通过，不代表真实引擎会话可靠 |
| 缺失 Pikafish 时降级 | PASS | 界面显示缺失提示且不崩溃 |
| 导师提示词构建 | PASS-LIMITED | 证据边界、最近走法截断、提示等级和视觉字段单测通过；尚未调用模型 |
| Windows 原生 GUI 启动 | PASS | `windows` Qt 平台创建 1366×768 窗口、处理事件并正常退出 |
| 依赖一致性 | PASS | `pip check` 无损坏依赖 |

## 第一阶段：实际运行与完整测试对局

原生 Windows GUI 已真实启动。启动、中文显示、标签页、分栏和缺失引擎提示可见。

完整测试对局：**FAIL / BLOCKED BY IMPLEMENTATION**。棋盘是占位控件，没有棋子、Position 状态或输入事件。以下项目均无法执行：棋子选择、移动、吃子、将军、应将、悔棋、重做、进入/退出沙盘、Pikafish 分析、真实 MultiPV、AI 讲解、棋谱播放、局面跳转和 AI 聊天。

已修复：原工具栏七个入口没有绑定行为却保持可点击，容易制造“功能存在”的假象；现已禁用并显示“尚未实现”的提示，状态栏改为“技术预览版”。

## 第二阶段：象棋规则专项验收

**全部 NOT IMPLEMENTED。** `board` 模块只有包说明，没有 Piece、Position、合法着生成或攻击检测。

| 项目 | 状态 |
|---|---|
| 马腿、象眼、炮架、兵卒过河 | NOT IMPLEMENTED |
| 仕/士九宫、帅/将九宫 | NOT IMPLEMENTED |
| 将帅照面、将军时应将约束 | NOT IMPLEMENTED |
| 被钉住棋子、吃将、无合法着 | NOT IMPLEMENTED |

这些是最高优先级发布阻断项。在全部规则测试通过前，不应继续扩展训练或 AI 功能。

## 第三阶段：Pikafish 可靠性

**FAIL / NOT IMPLEMENTED。** 本机未安装 Pikafish 或匹配 NNUE。项目只有路径发现和一条 `info` 解析器，没有子进程、UCI 握手、`position`、`go`、停止、超时、崩溃恢复或结果聚合。因此不能声称完成 20 个 FEN 验收。

额外风险：当前模型没有明确记录“引擎分数属于行棋方视角还是固定红方视角”的规范化字段。若直接接入导师，黑方优势极易被解释成红方优势。必须引入 `score_pov` 和转换后的 `score_for_red`，并分别测试红走与黑走。

## 第四阶段：AI 导师质量

**FAIL / PARTIAL。** 已增加通用 OpenAI-compatible HTTP Provider，并使用用户授权的百炼默认业务空间凭据对 `qwen-plus` 做真实最小调用：HTTP 200，响应约 4.99 秒，模型正确拒绝在没有引擎分析时断言最佳着。API Key 仅在测试进程内读取，没有写入源码、环境文件、日志或报告。仍没有凭据管理界面、重试、缓存、响应结构校验或模型调用记录，也无法进行 20 个具备真实 Pikafish 证据的局面质量抽检。

当前传输结构仍不足以可靠防止幻觉：缺少逐格棋子清单、合法着集合、规则合法性结论、走前/走后两个 FEN、评分视角、候选着中文译名、PV 每步对应 FEN、当前分支 ID 与来源标识。仅加强 Prompt 不能解决这些缺口。

## 第五阶段：“为什么不能这么走”

**NOT IMPLEMENTED。** 缺少 played move 的走后再分析、对手最佳回应、PV 对齐和损失原因提取器，无法测试 10 个次优走法。后续实现必须将“规则是否合法”和“棋力上是否较差”分开表示。

## 第六阶段：沙盘

**NOT IMPLEMENTED。** 没有树节点、父子关系、分支 ID、节点 FEN 或主线不可变保护。主谱不受影响、任意节点分支、无限延伸、切换不丢谱以及导师分支感知均未通过。

## 第七阶段：棋谱教学

**NOT IMPLEMENTED。** 数据库虽有 `games` 基础表，但没有合法数据、导入器、记谱解析器或播放服务，无法打开 10 盘棋谱。棋盘、回合、FEN、中文着法和历史一致性未验证。

## 第八阶段：UI 审查

原生 Windows 1366×768：启动和中文显示 PASS；当前只是占位布局。离屏测试覆盖 1920×1080、1600×900、1366×768，左右分栏未重叠。窗口最小尺寸设置为 1024×650。

未通过项：没有真实九路十行棋盘，无法检查棋盘比例；棋盘占位区域会随窗口任意拉伸；没有时间线、问题输入框、答案滚动、候选着列表或变化树内容；高 DPI 只设置了缩放策略，没有在 125%/150% 实机显示设置下验证。离屏平台缺少中文字体，但原生 Windows 截图中文正常。

## 第九阶段：性能与线程

| 指标 | 结果 |
|---|---|
| 模块导入 | 约 0.78 秒 |
| 数据库首次初始化 | 约 0.018 秒 |
| 窗口创建并显示 | 约 0.043 秒 |

当前没有引擎分析、HTTP 请求或大查询，因此不能验证对应耗时和主线程无冻结。数据库初始化目前发生在 Qt 主线程；现阶段耗时很短，但未来迁移和大查询必须移到工作线程。EngineWorker、HTTP 异步客户端和任务取消均未实现。

## 第十阶段：代码质量

通过项：最大 Python 文件约百行；没有硬编码用户绝对路径；没有裸 `except:`；SQL 初始化为静态语句，版本值参数化；API Key 未写入源码；当前没有 subprocess，因而没有现存僵尸进程。

问题与技术债务：

1. `app.py` 的顶层 `except Exception` 作为 GUI 最终边界合理，但必须配合更细粒度业务异常。
2. SQLite schema 直接内嵌在连接类，后续需要版本化迁移。
3. `TutorRequest.context` 使用宽泛的 `dict[str, object]`，缺乏可验证的强类型棋盘证据模型。
4. 没有依赖锁文件、持续集成、覆盖率门槛和发布版本信息页面。
5. 尚未设计引擎进程所有权、取消协议、退出清理和单实例约束。

## 第十一阶段：教学体验

核心系统提示词规定了 Hint 1 → Hint 2 → Hint 3 → Answer，但 UI 没有训练状态机和答案解锁控件，无法证明用户获得先思考的机会。当前也不会主动泄露最佳着，因为所有教学交互尚未实现。

后续正确状态机必须是：自己思考 → 思路提示 → 局部提示 → 候选着 → 用户明确请求后显示最佳着 → 原理解释 → 沙盘验证。每一级解锁应记录学习事件，并允许用户停在任意一级。

## 已修复的问题

- 禁用七个无实现的工具栏入口，避免误导用户。
- 状态栏明确标注“技术预览版”。
- 保留缺失 Pikafish 时的可理解提示和无崩溃启动。
- 原生平台复核后确认中文显示正常；离屏截图方框不再误判为程序字体缺陷。
- 已实现并测试 OpenAI-compatible Provider 的成功响应和安全错误转换；密钥不会出现在返回对象或用户错误信息中。

## 本地运行与打包结论

本地源码运行：PASS。项目虚拟环境使用 PySide6 6.8.3，可通过 `.\.venv\Scripts\python.exe -m xiangqi_tutor` 启动。

打包：本轮不作为交付门槛。审计期间曾用 PyInstaller 6.22.3 构建目录版，构建成功但运行时出现 QtCore DLL 装载失败，因此未将该产物标记为可运行版本。用户随后明确允许“不打包，本地能运行即可”，故停止继续处理打包兼容性；现有 `dist` 目录属于失败的审计产物，不应对外分发。

## 百炼 API 实测

- 凭据来源：用户明确授权的本地 CSV；未复制到仓库、`.env`、日志或报告。
- 协议：阿里云百炼 OpenAI-compatible Chat Completions。
- 选择模型：`qwen-plus`，适合通用中文解释任务。
- 结果：HTTP 200，约 4.99 秒。
- 质量烟雾测试：在 `engine_analysis` 为空时询问能否断言最佳着，模型明确拒绝猜测具体着法，符合证据边界。
- 限制：这只证明端点、凭据和基本提示词边界可用；没有 Pikafish 数据时，不能替代 20 个真实局面的导师质量验收。

## 尚未解决的发布阻断项

1. 完整规则层与规则测试。
2. 可交互棋盘与完整一盘对局。
3. FEN、走法坐标、中文记谱双向转换。
4. Pikafish UCI 后台服务及 20 FEN 交叉验证。
5. 强类型导师证据包、LLM Provider 和 20 局面质量验收。
6. “为什么不能这么走”的端到端反驳链。
7. 不破坏主谱的沙盘变化树。
8. 许可可追踪棋谱导入、播放和 10 盘一致性测试。
9. 原生多分辨率和高 DPI 人工验收。

## 下一阶段推荐开发功能

只推进一个优先方向：**Phase 2 完整象棋规则核心**。在马腿、象眼、炮架、过河、九宫、照面、应将、钉住、吃将与无合法着全部通过前，不开发聊天、训练、棋谱库或视觉装饰。规则完成后再实现 FEN/记谱，因为引擎和棋谱验收都依赖可信的 Position。

## 最终回归

- 单元与现有集成测试：12/12 PASS。
- Python 全包字节码编译：PASS。
- 依赖一致性检查：PASS，无损坏依赖。
- 原生 Windows GUI：PASS；Qt `windows` 平台、PySide6 6.8.3、1366×768、窗口可见、事件循环正常。
- 完整对局/规则/引擎/棋谱/沙盘端到端测试：仍为 NOT IMPLEMENTED，不因骨架测试通过而改变结论。
