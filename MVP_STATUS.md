# MVP 状态

更新日期：2026-09-14

## 已实际完成

- 真实九路十行棋盘与 32 枚初始棋子。
- 点击选择、合法落点、走棋、吃子、红黑轮换、非法着拒绝、最后一步和将军显示。
- 马腿、象眼、不过河、九宫、炮架、兵卒过河、将帅照面、应将、牵制、禁止吃将、将死与无合法着。
- 新对局、悔棋、重做、翻转、FEN 导入/导出、中文棋谱和历史同步。
- 20 个以上半回合的连续走棋及完整 undo/redo 自动测试。
- UCI 服务、Fake UCI 端到端测试、MultiPV 和红方固定视角评分。
- 后台引擎/AI 工作线程、按 FEN 丢弃过期分析结果。
- Pikafish 与 AI 设置、Windows keyring 密钥保存、连接测试。
- 强类型 TutorEvidence，包含 FEN、棋子清单、合法着、最近着、候选着、评分与 PV。

## 测试结果

- pytest：33 项通过（本文件生成前的回归结果）。
- Python 全包编译：通过。
- Fake UCI：握手、配置、分析、bestmove、MultiPV、退出通过。
- GUI 自动交互：启动、工具栏、真实点击走棋、历史更新、翻转前后坐标往返通过。

## 外部依赖验收

- 本机未发现可运行 Pikafish，因此真实引擎 20 FEN 验收尚未完成；Fake UCI 只证明协议与 GUI 接线。
- 用户授权的百炼 OpenAI-compatible 接口此前以 qwen-plus 实测 HTTP 200，约 4.99 秒；密钥未写入仓库。当前 UI 已接通同一 Provider，但尚未完成带真实 Pikafish 证据的 20 局面导师质量抽检。

## 尚未实现或未验收

- 沙盘变化树、任意历史节点分支、棋谱库/播放、残局题库、训练统计、自动整盘复盘。
- 真实 Pikafish 20 FEN 可靠性矩阵。
- 10 个次优走法的双阶段归因教学流水线。
- 1920×1080、1600×900、1366×768 与 125%/150% DPI 的完整人工矩阵（当前完成原生 Windows 启动和自动布局烟雾测试）。

## 已知限制

- 中文记谱支持常见走法与两枚同路车/炮/马/兵的前后消歧；更复杂的多兵同路编号仍待扩展。
- EngineService 支持长生命周期，但 GUI 当前每次分析创建并清理一个 UCI 进程，尚未复用单一进程。
- AI 输出当前作为文本显示，结构化箭头/高亮 JSON 尚未解析回棋盘。
